# Examples

Each example is a self-contained Lisp script. They expect the `opendaq` system to
be available to Quicklisp - clone this repository into `~/quicklisp/local-projects/opendaq/`.

## Running an example

From the repository root:

```bash
sbcl --non-interactive --load examples/streamreader.lisp
```
